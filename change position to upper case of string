#include <stdio.h>
#include<string.h>
int main() {
    char str[20];
    int i,j,po;
    printf("enter string ");
    gets(str);
    printf("which position you will change to upper case ");
    scanf("%d",&po);
    j=strlen(str);
    for(i=0;i<j;i++)
    {
       if(i==po-1)
       {
          str[i]=str[i]-32;
           
           break;
       }
    }
        printf("%s",str);

    return 0;
}

output

enter string kiet
which position you will change to upper case 3
kiEt
